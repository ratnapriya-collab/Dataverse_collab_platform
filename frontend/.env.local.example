# Copy this file to .env.local and adjust for your environment.
# In Vercel, set these as Project Environment Variables instead.

# Backend base URL — no trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
